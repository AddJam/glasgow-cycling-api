language: ruby
rvm:
  - 2.1.1
env:
  - CI_REPORTS=shippable/testresults
before_script:
  - psql -c 'create database journey_test;' -U postgres
services:
    - redis
script: rake test
notifications:
  email:
    - team@codecreatedme.com
