language: ruby
rvm:
  - 2.1.1
env:
  - CI_REPORTS=shippable/testresults
before_script:
  - psql -c 'drop database if exists journey_test;' -U postgres -w
  - psql -c 'create database journey_test;' -U postgres -w
services:
    - redis
script: bundle exec rake test
notifications:
  email:
    - team@codecreatedme.com
